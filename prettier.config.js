module.exports = {
    printWidth: 100,//折り返す行の長さを指定
    tabWidth: 2,//インデントの幅を指定
    useTabs: false,//インデントにタブを使うかスペースを使うかを指定
    semi: true, //文末にセミコロンを付けるかどうかを指定
    singleQuote: false, //文字列をシングルクォートで囲むかどうかを指定
    quoteProps: "as-needed", //オブジェクトのプロパティ名をクォートで囲むかどうかを指定
    jsxSingleQuote: false, //JSXの属性値をシングルクォートで囲むかどうかを指定
    trailingComma: "es5", //末尾のカンマをどのようにするかを指定
    bracketSpacing: true,   //オブジェクトリテラルのブラケットの前後にスペースを入れるかどうかを指定
    bracketSameLine: false, //オブジェクトリテラルのブラケットを改行するかどうかを指定
    arrowParens: "always",  //アロー関数の引数にカッコをつけるかどうかを指定
    proseWrap: "preserve",  //Markdownのテキストを折り返すかどうかを指定
    htmlWhitespaceSensitivity: "css",     //HTMLの空白を認識するかどうかを指定
    endOfLine: "lf",    //改行コードを指定
    embeddedLanguageFormatting: "off",  //埋め込みコードのフォーマットを指定
}